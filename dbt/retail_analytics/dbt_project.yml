name: retail_analytics
version: "1.0.0"
profile: retail_analytics

model-paths: ["models"]
test-paths: ["tests"]

models:
  retail_analytics:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
